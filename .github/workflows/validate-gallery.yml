name: Validate gallery submissions

on:
  pull_request:
    paths:
      - 'gallery/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run submission validator
        run: |
          node ./tools/validate_submission.js
